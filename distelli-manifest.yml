altest26/NodeExpress:
  PkgInclude:
    - '*'

  PkgExclude:
    - node_modules/

  PreRelease:
    - npm install
    - npm test

  PreInstall:
    - echo "Begin PreInstall"
    - sudo apt-get update
    - sudo apt-get <in></in>stall nodejs -y
    - sudo apt-get install npm -y

  PostInstall:
    - echo "Begin PostInstall"

  Exec:
    - echo "Begin Exec"
    - /usr/bin/nodejs app.js
